export type articleProps = {
  title: string;
  description: string;
  profileImage?: string;
  created_by: string;
  created_timestamp: string;
};

export type mediumEditorProps = {
  style: string;
  placeholder: string;
};
